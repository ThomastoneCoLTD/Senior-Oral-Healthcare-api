# 설문 영역별 답변 초기화 검증 및 배포 결과

- 일자: 2026-09-21, 환경: prod.
- API 기능 commit: `48548fa8b3a119d96758a0a7a69c5bba7a70a955`.
- Frontend 기능 commit: `1d4624883995c76d9d577126fb600b8b11e40359`.
- API workflow: https://github.com/ThomastoneCoLTD/Senior-Oral-Healthcare-api/actions/runs/35541173549 — success.
- API ASG refresh: `a1b8dfe0-ec1b-4ce3-8fde-98ba99b630e1` — Successful, 100%. Workflow가 refresh를 시작한 것과 실제 교체 완료를 별도로 확인했다.
- Frontend workflow: https://github.com/ThomastoneCoLTD/Senior-Oral-Healthcare-front/actions/runs/35541486821 — success.
- 운영 확인: API `/api/actuator/health`는 `UP`; 웹 HTTP 200. 실제 배포 JS `/assets/index-XjOIMH0s.js`에 영역별 초기화 버튼 및 확인 창 문구가 포함됨을 확인했다.

## 검증

- Frontend Vitest: 관련 2개 파일, 31개 테스트 통과. 변경 TypeScript ESLint 통과, production build 통과.
- Backend: Java 17.0.17, IntakeSurveyTest 41개 통과, bootJar 통과. 기존 Java 경고 및 프론트 번들 크기 경고는 남아 있다.
- 브라우저: Edge, 390/1180/1440px × 7영역 = 21개 조합 통과. 태블릿 130% 글자 크기도 검사했다.
- 확인·취소, 영역 전체 초기화, 다른 영역 보존, 단일/복수/조건부/선택 응답, 레거시 방문 월, BMI 입력 초기화, 미응답 이동 제한, 저장 중 비활성화, 저장 실패·재시도 및 새로고침을 모의 API로 검사했다.
- 휴대폰·태블릿 확인 창 및 PC 초기화 후 화면을 시각 검토했다. 실제 사용자 답변을 수정하는 운영 테스트는 하지 않았다. 실제 iPad Safari 기기 검증은 미수행이다.
- 변경기록 DOCX는 양쪽 저장소에 동일하게 보관했다. 번들 렌더러는 Windows의 LibreOffice 부재로 실행되지 않아 Word PDF 변환과 번들 Poppler로 2페이지를 렌더링하여 확인했다.
- 기존 작업본의 미커밋 변경과 브랜치는 보존했다. 두 저장소 최신 prod에서 분리한 작업 폴더에 요청 파일만 커밋·푸시했다.

## 영향 및 복구

문구 두 곳과 사용자 설문 UI만 변경했다. API/DB/Secret/인프라 계약, 문항 ID, 점수 및 완료 응답은 유지한다. 초기화는 현재 영역의 응답만 비우고 임시저장으로 저장한다. 필요 시 기능 commit을 revert하여 프론트부터 배포한다. 사용자 초기화 후 저장된 이전 응답은 코드 revert만으로 복원되지 않는다.